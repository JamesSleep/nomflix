import React from "react";
import { SearchProps } from "../../Interfaces";

export default ({ movieResults, tvResults, searchTerm, loading, error, handleSubmit }: SearchProps) => (
  <div>Search</div>
)